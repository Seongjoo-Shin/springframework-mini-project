<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.webapp.dao.MypageDao">

	<!-- 유저 비밀번호 검색 -->
	<select id="selectPassword" resultType="String">
		select user_password from users where user_id = #{userId}
	</select>
	
	<!-- 유저 비밀번호 수정 -->
	<update id="updatePassword" parameterType="user">
		update users set user_password = #{userPassword} where user_id = #{userId}
	</update>
	
	<!-- 유저 탈퇴 -->
	<update id="userWithdrawal" parameterType="user">
		update users set user_enabled = 0 where user_id = #{userId}
	</update>
</mapper>